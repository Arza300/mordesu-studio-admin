-- ============================================================
-- سكربت تحديث آمن — لا يحذف أي بيانات ولا أي مستخدمين
-- استخدم هذا السكربت عندما تريد إضافة أعمدة أو تعديلات فقط
-- يشغّل في Neon SQL Editor (اختر قاعدة neondb)
-- ============================================================

-- إضافة عمود سعر المميزات والتعديلات لجدول العملاء (إن لم يكن موجوداً)
ALTER TABLE "Client" ADD COLUMN IF NOT EXISTS "featuresModificationsPrice" INTEGER NOT NULL DEFAULT 0;
